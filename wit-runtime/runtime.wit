package runtime:runtime;

interface host-functions {
    return-ok: func();
    return-err: func();
    panic: func(host: bool);
}

world example {
    import host-functions;
    export host-functions;
}
